import * as d from '../../declarations';


export const build = async (_config: d.Config, _compilerCtx: d.CompilerCtx) => {
  const buildResults: d.CompilerBuildResults = {

  } as any;
  return buildResults;
};
